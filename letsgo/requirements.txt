# ─── Computer Vision & AI ────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0

# ─── Raspberry Pi Camera ────────────────────────────────────────
picamera2

# ─── GPIO & Servo Control ───────────────────────────────────────
# NOTE: RPi.GPIO is NOT compatible with Pi 5 (BCM2712 / RP1 chip)
gpiozero
lgpio
pigpio

# ─── Google Gemini API ──────────────────────────────────────────
google-genai>=1.0.0

# ─── Dataset / Model Download ───────────────────────────────────
kagglehub

# ─── Hailo AI HAT+ (system packages, not pip-installable) ───────
# Install via:  sudo apt install hailort
# hailo_platform Python bindings are bundled with hailort
# libhailort and the PCIe driver must also be installed:
#   sudo apt install hailort-pcie-driver
